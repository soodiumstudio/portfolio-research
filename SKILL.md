---
name: portfolio-research
description: 관심 분야에 어떤 서비스가 이미 있는지 조사한다. 실행하면 무엇을 알아볼지·누가 쓸지·어디까지 볼지를 선택지로 먼저 물어 조사 범위를 확정한 뒤 시작하고, 진행률을 실시간으로 알려준다. 결과는 시장 숫자·비교표·비어 있는 자리 세 가지와 중학생도 읽을 수 있는 한 장 요약으로 나온다. "시장 조사", "경쟁 서비스 분석", "비슷한 앱 찾아줘", "이 분야 조사해줘", "데스크 리서치", "리서치 어떻게 시작하지" 같은 요청에 사용한다.
---

# 남들은 뭘 만들었나 찾아보기

학생은 대부분 "이런 거 만들고 싶어요" 한 줄만 들고 온다. 거기서 시작해서 쓸 수 있는 조사 결과까지 데려가는 것이 이 스킬의 일이다.

---

## 0단계 — 시작하자마자 이 안내를 그대로 보여준다

학생은 이 스킬이 무엇을 해 주는지 모른 채 실행한다. 질문부터 던지지 말고 아래를 먼저 출력한다.

```
📌 이 스킬이 하는 일
   관심 있는 분야에 이미 어떤 서비스가 있는지 찾아서,
   포트폴리오에 바로 붙일 수 있는 파일 세 개로 만들어 드립니다.

📌 이럴 때 쓰세요
   · "이런 거 만들어 볼까" 생각만 있고 아직 아무것도 안 정했을 때
   · 만들고 싶은 건 있는데, 남들은 어떻게 했는지 모를 때
   · 포트폴리오에 넣을 리서치 페이지가 필요할 때

📌 이건 못 합니다
   · 사용자 인터뷰 — 사람을 직접 만나야 합니다
   · 앱을 실제로 써 본 느낌 — 직접 깔아서 써 봐야 합니다
   · 아무도 조사한 적 없는 숫자 — 없는 건 없다고 말씀드립니다

📌 준비물
   없습니다. "이런 거 만들고 싶어요" 한 줄이면 시작합니다.

📌 걸리는 시간
   질문 고르기 1분  →  조사 20~40분  →  정리 5분
   진행률을 계속 알려드리니 창을 닫지 마세요.

그럼 시작할게요. 네 가지만 골라 주시면 바로 조사합니다.
```

수업 중이라면 여기에 덧붙인다:
> 오늘 수업에서는 **질문 고르기까지만** 합니다. 조사 실행은 집에서 하세요. 여럿이 동시에 돌리면 중간에 멈춥니다.

---

## 시작 게이트 — 준비 확인부터 한다 (건너뛰지 않는다)

> ⛔ **학생이 "완료"를 고르기 전에는 아무것도 시작하지 않는다.**
> 주제를 인자로 받았어도 마찬가지다. 학생마다 컴퓨터 상태가 달라서,
> 준비가 안 된 채로 시작하면 중간에 멈추고 처음부터 다시 해야 한다.

**1) 아래 카드를 그대로 출력한다.**

```
📋 시작 전에 세 가지만 확인해 주세요

① 지금 열려 있는 폴더가 맞나요?
   조사 결과 파일이 이 폴더에 저장됩니다.
   매주 같은 폴더를 여셔야 지난주 결과가 이어집니다.

② 인터넷이 되나요?
   자료를 찾아야 해서 인터넷이 필요합니다.

③ 30분 정도 시간이 있나요?
   중간에 창을 닫으면 처음부터 다시 해야 합니다.
   진행률을 계속 알려드리니 얼마나 남았는지 보실 수 있습니다.

✨ 선택 — 더 깊게 조사하고 싶다면
   insane-research 를 미리 설치해 두면 훨씬 정확해집니다.
   없어도 됩니다. 없으면 웹 검색으로 진행합니다.
```

**2) 그다음 `AskUserQuestion` 으로 확인받는다.**

```
header: "준비"
question: "준비되셨나요?"
options:
  - "네, 시작할게요"        (설명: 바로 조사 범위를 정하는 질문으로 넘어갑니다)
  - "폴더가 뭔지 모르겠어요"  (설명: 어디에 저장되는지 먼저 알려드릴게요)
  - "시간이 없어요"          (설명: 10분 안에 끝나는 간단 모드로 해드릴게요)
  - "insane-research 설치할래요" (설명: 설치 방법을 알려드리고 기다릴게요)
```

각 선택지에 맞게 대응하고, 해결되면 **다시 이 질문으로 돌아온다.**

---

## 학생 컴퓨터가 윈도우일 수 있다 (명령 내기 전에 확인)

**어떤 컴퓨터인지 먼저 확인하고 그에 맞는 명령만 준다.** 환경 정보에 있고, 없으면 `uname -s` 로 본다.
맥 명령을 윈도우 학생에게 주면 오류만 보고 거기서 막힌다. 학생은 왜 안 되는지 모른다.

| | 맥 · 리눅스 | 윈도우 (PowerShell) |
|---|---|---|
| 스킬 폴더 | `~/.claude/skills/` | `$env:USERPROFILE\.claude\skills\` |
| 파일이 있나 | `test -f "경로"` | `Test-Path "경로"` |
| 명령이 있나 | `command -v node` | `Get-Command node -ErrorAction SilentlyContinue` |
| 스크립트 실행 | `bash 파일.sh` | `powershell -ExecutionPolicy Bypass -File 파일.ps1` |

> ⚠️ **윈도우 학생에게 `~` 를 주지 않는다.** PowerShell 에서는 동작하지만
> cmd 창에서는 `~` 라는 폴더가 새로 생겨서 엉뚱한 데 설치된다.
>
> ⚠️ **WSL 안의 `bash` 를 쓰지 않게 한다.** WSL 의 홈은 윈도우 홈과 달라서,
> 거기 설치하면 Claude Code 가 스킬을 찾지 못한다.

---

## 진행률 — 매 단계마다 반드시 출력한다

학생은 조사가 얼마나 남았는지 모르면 창을 닫는다. **단계가 바뀔 때마다 아래 한 줄을 먼저 출력한다.**

```
▓▓▓░░░░░░░ 30%  |  조사 설계서 만드는 중
```

| 시점 | % | 붙이는 말 |
|---|---|---|
| 안내 카드 출력 직후 | 5% | 질문 준비 중 |
| 질문 답변 받은 직후 | 15% | 조사 범위 정리 중 |
| 학생이 확인해 준 직후 | 25% | 조사 설계서 만드는 중 |
| 조사 시작 | 35% | 자료 찾는 중 (여기가 제일 깁니다) |
| 조사 중간 보고 | 50% / 65% | 지금까지 찾은 것 N건 |
| 조사 끝 | 75% | 숫자 확인하는 중 |
| 숫자 검증 끝 | 85% | 요약 쓰는 중 |
| 00-요약.md 저장 | 95% | 근거 파일 만드는 중 |
| 전부 저장 | 100% | 끝났습니다 |

조사(35~75%) 구간이 길다. **중간에 최소 두 번은 진행률과 함께 지금까지 찾은 것을 알린다.**
막대는 10칸 기준으로 `▓` 와 `░` 를 쓴다.

---

## 물어보는 방식 — 학생은 고르기만 한다

학생은 **"해줘" 한 마디만 한다**고 가정한다. 자세히 설명해 달라고 하지 않는다.

1. **질문은 반드시 `AskUserQuestion` 도구로 한다.** 글로 나열하지 않는다.
   글로 물으면 학생이 한 줄로 뭉뚱그려 답하고, 그러면 안 물어본 것과 같다.
2. **선택지는 내가 만든다.** 받은 자료를 먼저 보고 그럴듯한 후보를 만든다.
   학생에게 백지에 서술하라고 시키지 않는다.
3. **한 단계에서 질문은 한 번만.** 성격이 다른 질문이라도 한 번의 호출에 묶어서 묻는다.
   단계가 바뀌면 다시 물어도 된다 — 앞 단계 결과가 있어야 물을 수 있는 것들이 있다.
   **시작하기 전에 연달아 세 번 넘게 묻지 않는다.** 그 전에 못 채운 것은 추측하고
   "이렇게 가정했습니다"라고 알린다.
3-1. **정리해서 읽어 주는 것은 질문이 아니다.** 답을 모아 한 문단으로 읽어 주고
   "다르면 말씀해 주세요"를 덧붙이는 건 호출 횟수에 넣지 않는다.
   **답을 기다리지 말고 바로 다음으로 넘어간다.** 학생이 고치면 그때 반영한다.
   여기서 멈춰 서면 학생은 뭘 해야 하는지 모른 채 창을 닫는다.
4. **모든 질문에 빠져나갈 선택지를 넣는다.** — "잘 모르겠어요" 또는 "알아서 해주세요".
   이게 없으면 학생은 아무것도 고르지 않고 창을 닫는다.
5. **선택지 설명에 결과를 적는다.** "이걸 고르면 무엇이 달라지는지"를 한 줄로.
   예) `직장인 견주 (설명: 산책 시간을 못 내는 문제로 좁혀집니다)`
6. **한 번에 한 주제만.** 성격이 다른 질문을 한 화면에 섞지 않는다.
7. **학생이 "몰라요", "알아서 해줘"라고 하면 더 묻지 않는다.** 바로 가정 모드로 간다.

**너무 자세히 묻지 않는다.** 학생이 답을 모르는 질문은 묻는 게 아니라 내가 정하고 확인만 받는다.

---

## 1단계 — 질문 4개를 선택지로 즉시 묻는다 (절대 건너뛰지 않는다)

> ⛔ **인자로 주제를 받았더라도 질문을 건너뛰지 않는다.**
> "반려견 산책 앱 시장 조사해줘" 같은 한 줄은 **1번 질문의 답일 뿐이다.**
> 나머지는 모른다. 모르는 채로 조사하면 학생이 원하지 않는 결과가 나온다.
> 조사를 먼저 시작하는 것은 이 스킬의 실패다.

**반드시 `AskUserQuestion` 도구를 호출한다.** 글로 질문을 나열하지 않는다.
글로 물으면 학생이 한 줄로 뭉뚱그려 답하고, 그러면 안 물어본 것과 같아진다.

### 주제가 아직 없을 때

**이것도 `AskUserQuestion` 으로 묻는다.** 글로 물으면 학생이 "잘 모르겠는데요"로 끝낸다.
예시 주제 셋과 "직접 쓸게요"를 선택지로 준다. 자기 주제는 기타 칸에 적는다.

```
header: "주제"
question: "어떤 분야에 관심이 있으세요?"
options:
  - "자취생 밥 챙겨 먹기"  (설명: 혼자 사는 사람의 식사 문제로 조사합니다)
  - "중고거래 사기 막기"    (설명: 중고거래 앱의 안전 문제로 조사합니다)
  - "어르신 키오스크"       (설명: 고령층의 디지털 이용 문제로 조사합니다)
  - "직접 쓸게요"          (설명: 기타를 눌러 관심 분야를 한 줄로 적어 주세요)
```

### 질문 예산 — 시작 전 세 번

| 몇 번째 | 무엇 | 언제 |
|---|---|---|
| 1 | 시작 게이트 "준비되셨나요?" | 항상 |
| 2 | 주제 | **인자로 주제를 받았으면 건너뛴다** |
| 3 | 조사 범위 네 가지 | 항상, 한 번에 묶어서 |

주제를 받고 시작했으면 2번이 비므로 여유가 한 번 생긴다.
그래도 **나머지 네 가지는 반드시 한 번에 묻는다.** 하나씩 물으면 학생이 중간에 지친다.

### 주제가 있을 때 — 아래 4개를 한 번에 묻는다

`AskUserQuestion` 을 **한 번** 호출하고, 질문 4개를 함께 넣는다.
선택지는 **주제에 맞게 그때그때 만들어 낸다.** 아래는 "반려견 산책 앱" 예시다.

**질문 1 · 무엇을 알아볼까요** (header: `조사 목적`)
| 선택지 | 설명 |
|---|---|
| 왜 기존 앱이 안 쓰이나 **(추천)** | 이미 나온 산책 앱들이 왜 자리 잡지 못했는지. 빈틈이 여기서 나옵니다 |
| 얼마나 많은 사람이 겪나 | 반려견 산책과 관련해 어떤 어려움이 얼마나 있는지 숫자로 |
| 누가 이미 하고 있나 | 비슷한 앱들을 모아 비교표로 |
| 해외는 어떻게 하나 | 국내에 아직 없는 방식을 찾을 때 |

첫 번째를 **(추천)** 으로 맨 앞에 둔다. 채점표에서 점수가 가장 잘 나오는 형태다.

**질문 2 · 누가 쓸 서비스인가요** (header: `대상`)
주제에 맞는 후보를 4개 만든다. 나이와 상황이 함께 들어가야 한다.
| 선택지 | 설명 |
|---|---|
| 처음 키우는 1~2년 차 | 뭘 해야 하는지 몰라 불안한 시기 |
| 직장인 견주 | 산책 시간을 내기 어려운 사람 |
| 노령견 보호자 | 산책 강도를 조절해야 하는 사람 |
| 대형견 보호자 | 산책 난이도와 민원이 다른 경우 |

**질문 3 · 어디까지 볼까요** (header: `범위`)
| 선택지 | 설명 |
|---|---|
| 한국만 | 포트폴리오라면 보통 이걸로 충분합니다 |
| 한국 + 해외 | 국내에 없는 아이디어를 찾고 싶을 때 |

**질문 4 · 알고 있는 비슷한 앱이 있나요** (header: `아는 서비스`, `multiSelect: true`)
검색해서 실제 후보를 4개 찾아 넣는다. 지어내지 않는다.
| 선택지 | 설명 |
|---|---|
| (실제 앱 이름) | 무엇을 내세우는 앱인지 한 줄 |
| … | |
| 모르겠어요 | 제가 찾아서 보여드릴게요 |

**답을 받으면 이렇게 한다**
- 학생이 "기타"로 직접 쓴 답이 있으면 그것을 우선한다.
- 4개 답을 **한 문단으로 정리해 읽어 준다.** "다르면 말씀해 주세요"를 덧붙이고 바로 조사로 넘어간다. 답을 기다리지 않는다.
- 질문 1의 답이 조사의 중심 질문이 된다. 같은 걸 다시 묻지 않는다.

---

## 2단계 — 조사한다. 단, 두 갈래로 나눠서

**이 구분을 학생에게 반드시 설명한다.** 안 그러면 학생은 AI가 다 해 준 줄 안다.

| | 검색으로 알 수 있는 것 | 직접 켜 봐야 아는 것 |
|---|---|---|
| 시장 숫자 | ⭕ 통계, 보도자료 | |
| 어떤 서비스가 있는지 | ⭕ | |
| 가격·요금제 | ⭕ | |
| 첫 화면에서 무엇을 시키는지 | | ⭕ |
| 끝내는 데 몇 번 눌러야 하는지 | | ⭕ |
| 어디서 막히고 짜증 나는지 | | ⭕ |

**AI가 하는 것** — 후보 목록, 비교 기준, 검색으로 채울 수 있는 칸까지.
**학생이 하는 것** — 직접 깔아서 나머지 칸 채우기. 숙제로 남긴다.

비교표의 모든 칸에 표시를 남긴다: `🔍 직접 확인` / `📄 검색으로만`

### 2-1. 조사 엔진이 있는지 확인한다 (추측하지 않는다)

> ⛔ **스킬 목록에 보이는지로 판단하지 않는다.** 방금 설치했거나 방금 지운 경우 목록 갱신이 늦어
> 틀린 판단을 한다. **파일로 확인한다.**

**맥 · 리눅스**
```bash
test -f ~/.claude/skills/insane-research-main/SKILL.md && echo OK || echo MISSING
```
**윈도우 (PowerShell)**
```
Test-Path "$env:USERPROFILE\.claude\skills\insane-research-main\SKILL.md"
```
(`True` 면 OK, `False` 면 MISSING)

- **`OK`** → `insane-research-main` 스킬을 호출해 조사한다. 여러 조사원이 나눠 찾고 서로 검증한다.
- **`MISSING`** → 아래 안내를 출력하고 **멈춘다.** 학생이 설치하거나 "간단하게"라고 할 때까지 진행하지 않는다.

학생이 "설치했어요"라고 하면 **같은 명령으로 다시 확인한다.** `OK` 가 나오기 전에는 넘어가지 않는다.
`MISSING` 이 계속 나오면 앱을 껐다 켰는지 묻는다.

**없을 때는** 이렇게 안내한다. 조용히 검색으로 때우지 않는다.

```
깊이 있게 조사하는 엔진이 아직 설치되지 않았네요. 한 줄이면 됩니다.

터미널(검은 창)에 아래를 붙여넣고 엔터를 치세요.

  [맥]     bash ~/.claude/skills/portfolio-research/install-research-engine.sh

  [윈도우] powershell -ExecutionPolicy Bypass -File "$env:USERPROFILE\.claude\skills\portfolio-research\install-research-engine.ps1"

1~2분 걸립니다. 끝나면 Claude Code를 껐다 켜 주세요.

지금 그냥 해 보고 싶으시면 "간단하게"라고 말씀하세요.
웹 검색만으로 10분 안에 해 드립니다. 대신 깊이는 얕습니다.
```

> 이건 **터미널** 명령이다. 앞의 준비물 안내와 달리 채팅창이 아니다.
> 학생이 헷갈리므로 "검은 창"이라고 분명히 말해 준다.

**설치가 끝났다고 하면** 스킬 목록에 `insane-research-main` 이 보이는지 확인하고 진행한다.
안 보이면 앱을 껐다 켰는지 다시 묻는다.

### 2-2. 넘길 때는 "조사 설계서"를 만들어서 넘긴다

**"자취생 밥 앱 조사해줘" 처럼 넘기지 않는다.** 1단계 답을 아래 형태로 바꿔
`research/query.json` 에 저장하고, 그 내용으로 `insane-research-main` 을 호출한다.

```json
{
  "task": {
    "title": "자취 20대의 저녁 식사 관련 서비스 현황과 빈 자리",
    "objective": "이미 나온 서비스들이 왜 자취 20대에게 자리 잡지 못했는지 밝히고, 아직 비어 있는 기회 영역 3개를 뽑는다",
    "type": "exploratory"
  },
  "context": {
    "background": "혼자 사는 20대 직장인은 퇴근이 늦어 저녁을 거르는 경우가 많다. 간편식과 배달 서비스가 늘었지만 이 집단에 특화된 서비스가 자리 잡았는지는 확인되지 않았다. 포트폴리오의 문제 정의 근거로 쓸 자료가 필요하다.",
    "audience": "general",
    "use_case": "UX/UI 포트폴리오의 리서치 페이지 작성과 문제 정의의 근거로 사용",
    "prior_knowledge": ["배달의민족", "쿠팡이츠", "마켓컬리"]
  },
  "questions": {
    "primary": "이미 나온 식사 관련 서비스들이 자취하는 20대에게 자리 잡지 못한 이유는 무엇인가",
    "secondary": [
      "이 집단을 겨냥한 서비스는 어떤 것들이 있고 각각 무엇을 내세우는가",
      "요금 구조는 어떻게 되어 있는가",
      "이용자들이 공개적으로 남긴 불만은 무엇인가"
    ],
    "exclusions": [
      "직접 인터뷰해야만 알 수 있는 개인의 동기와 감정",
      "실제 앱을 써 봐야 아는 사용 경험",
      "실현 여부가 확인되지 않은 시장 예측치"
    ]
  },
  "constraints": {
    "timeframe": { "start": "2018", "end": "present", "focus_period": "최근 3년" },
    "geography": { "scope": "national", "regions": ["대한민국"] },
    "sources": {
      "required_types": ["official_docs", "industry_reports", "news"],
      "min_quality": "B",
      "language": ["ko", "en"],
      "preferred_domains": ["kostat.go.kr", "kdca.go.kr", "mfds.go.kr", "atfis.or.kr", "korea.kr"]
    }
  },
  "output": {
    "format": "comprehensive_report",
    "length": { "min_words": 1500, "max_words": 5000 },
    "structure": {
      "include_executive_summary": true,
      "include_bibliography": true,
      "include_methodology": true
    }
  }
}
```

**이 값들은 형식을 채우는 게 아니라 각각 사고를 하나씩 막는 장치다.** 반드시 채운다.

| 넣는 값 | 이게 막는 사고 |
|---|---|
| `timeframe` 은 넉넉히, `focus_period` 로 최근 강조 | 날짜로 잘라 **정부 실태조사 같은 A급 자료를 놓치는 것** |
| `exclusions` 에 "확인되지 않은 예측치" | 오래된 전망치가 현재 수치처럼 섞이는 것 |
| `sources.language` 에 `"ko"` 추가 | **기본값이 영어라서** 국내 통계를 아예 못 찾는 것 |
| `sources.min_quality: "B"` | 개인 블로그·커뮤니티 글이 근거로 올라오는 것 |
| `sources.preferred_domains` 에 정부 통계 사이트 | 출처 등급이 뒤섞이는 것 |
| `questions.exclusions` | 인터뷰로만 알 수 있는 것을 검색으로 답하려 드는 것 |

`sources.language` 를 비우면 영어 자료만 찾는다. **국내 주제라면 반드시 `"ko"` 를 넣는다.**

원본 리포트는 `research/raw/` 에 **그대로 남긴다.** 요약만 남기면 나중에 근거를 찾을 수 없다.

### 2-3. 간단 모드 (폴백)

insane-research 가 없거나 학생이 "간단하게"라고 하면 웹 검색으로 진행한다.
출처를 최소 다섯 개 확보하고, 결과 맨 위에 적는다.

> ⚠️ 이 조사는 간단 모드로 했습니다. 포트폴리오에 넣기 전에 출처를 직접 확인하세요.

**시간이 오래 걸린다는 것을 미리 알린다.** 수업 중이라면 1단계까지만 하고 실행은 나중에 하라고 안내한다.

---

## 3단계 — 숫자는 이렇게 확인한다 (사고가 제일 많이 나는 곳)

실제로 검색해 보면 아래 일들이 일어난다. 학생에게 이 예시를 그대로 보여준다.

```
⚠️ 실제로 있었던 일 1 — 서로 다른 걸 센 숫자가 섞여 나옵니다
   중고거래 사기를 찾으면 이 숫자들이 한꺼번에 나옵니다.
     · 피해구제 신청 175건    (플랫폼을 상대로 접수된 건, 1년치)
     · 직거래 사기 12만건     (플랫폼과 무관한 전체, 1년치)
     · 누적 피해액 1조 7천억   (5년을 다 더한 값)
   전부 다른 걸 센 숫자입니다. 그런데 눈에 들어오는 건 제일 큰 숫자입니다.
   "중고거래 사기 1조 7천억"이라고 써 두면,
   "그게 앱에서 난 사기예요?" 한 마디에 무너집니다.

⚠️ 실제로 있었던 일 2 — 오래된 예측이 지금 숫자처럼 보입니다
   "간편식 시장 10조"가 검색됩니다.
   찾아보면 2019년 자료의 '2025년이 되면 이럴 것이다' 예측입니다.
   실제로 그렇게 됐는지는 아무도 확인하지 않았습니다.

⚠️ 실제로 있었던 일 3 — 찾던 숫자가 없으면 옆 숫자가 옵니다
   "저녁 결식률"을 찾았는데 "아침 결식률 62.1%"가 나옵니다.
   다른 숫자입니다. 그냥 쓰면 아무도 모릅니다 — 면접 전까지는.
```

그래서 숫자마다 **네 가지**를 붙인다. 하나라도 없으면 그 숫자는 쓰지 않는다.

| 붙일 것 | 없거나 다르면 |
|---|---|
| **무엇을 센 숫자인가** | 쓰지 않는다. 가장 많이 나는 사고다 |
| **몇 년 자료인가** | 쓰지 않는다 |
| 실제로 잰 값인가, 예측인가 | `(예측치)` 라고 적는다 |
| 내가 알고 싶던 바로 그 숫자인가 | `(대신 찾은 숫자)` 라고 적는다 |

**서로 다른 지표를 나란히 놓지 않는다.** 꼭 함께 써야 하면 각 숫자 옆에 무엇을 센 것인지 반드시 적는다.

### 오래된 자료를 무조건 버리지 않는다

정부 실태조사는 몇 년에 한 번만 한다. **4년 지난 자료가 최신인 경우가 많다.**
키오스크 관련 가장 좋은 자료는 한국소비자원 2022년 실태조사인데,
"최근 3년"으로 잘라내면 이 A급 자료를 버리고 신문 기사만 남는다.

| 상황 | 이렇게 한다 |
|---|---|
| 오래됐지만 그게 최신 | 쓴다. `(2022년 조사, 이후 같은 조사 없음)` 이라고 적는다 |
| 더 최신 자료가 있는데 옛것을 찾았다 | 최신으로 바꾼다 |
| 오래된 **예측치** | 쓰지 않는다. 예측은 시간이 지나면 값이 아니다 |

학생에게 말한다: "오래된 게 문제가 아니라, 오래된 걸 최신인 척하는 게 문제입니다."

### 출처는 등급을 매겨 표시한다

| 등급 | 어디 | 포트폴리오에 |
|---|---|---|
| A | 정부·공공기관 (통계청, 한국소비자원, 한국지능정보사회진흥원, 질병관리청 등) | 씁니다 |
| B | 업계 리포트, 언론 보도 | 씁니다. 원자료를 찾으면 더 좋습니다 |
| C | 블로그, 커뮤니티, 요약 글 | 힌트로만. 그대로 인용하지 않습니다 |

숫자를 못 찾으면 **"찾지 못했습니다"라고 쓴다. 절대 지어내지 않는다.**
학생에게 말한다: "숫자가 없는 건 문제가 아닙니다. 없는 걸 있는 것처럼 쓰는 게 문제입니다."

## 4단계 — 학생이 읽을 수 있는 형태로 정리한다

조사 리포트를 그대로 주면 학생은 안 읽는다. **한 장 요약을 맨 앞에 만든다.**
전부 `research/` 아래에 저장한다.

### `research/00-요약.md` — 이것부터 만든다 (제일 중요)

학생이 실제로 읽는 파일이다. 표가 아니라 **문장**으로 쓴다.

```markdown
# 조사해 보니 이렇습니다

## 알게 된 것 세 가지
1. 60대 이상 절반 이상(53.6%)이 키오스크 조작을 어려워합니다.
   — 한국소비자원 키오스크 이용 실태조사, 2022년 (이후 같은 조사 없음)
2. 나이가 많을수록 심해집니다. 65~74세는 71.5%, 75세 이상은 76.6%입니다.
   — 같은 조사
3. 어려움의 종류가 조작만이 아닙니다. '뒷사람 눈치가 보인다'가 41.1%입니다.
   — 같은 조사

## 그래서 무슨 뜻인가요
숫자만 보면 "어르신이 키오스크를 어려워한다"로 끝납니다.
그런데 3번을 보면 문제가 기능이 아니라 시간에 쫓기는 상황일 수 있습니다.
버튼을 크게 만드는 것과, 뒷사람을 신경 쓰지 않게 만드는 건 완전히 다른 설계입니다.

## 포트폴리오에 이렇게 쓰세요
문제 정의 페이지에 이 문장을 그대로 붙이셔도 됩니다.
> 65~74세의 71.5%가 키오스크 이용에 불편을 겪는다. 그중 41.1%는
> '뒷사람 눈치'를 이유로 꼽았다. (한국소비자원, 2022)

## 조심할 것
- 2022년 조사입니다. 이후 같은 조사가 없어 이게 최신입니다. 연도를 반드시 함께 쓰세요.
- '디지털 정보화 수준 69.9'라는 숫자도 나오는데, 이건 키오스크가 아니라
  디지털 전반을 잰 다른 숫자입니다. 섞어 쓰지 마세요.

## 아직 모르는 것 (직접 하셔야 합니다)
- 어르신이 키오스크 앞에서 실제로 어디서 멈추는지 — 직접 보거나 물어봐야 압니다
- 매장이 왜 키오스크를 두는지 — 점주 입장은 이 조사에 없습니다
```

**"그래서 무슨 뜻인가요" 칸을 반드시 채운다.** 숫자를 나열하는 것과 숫자를 읽어 주는 것은 다르다.
학생이 스스로 해석하지 못하면 포트폴리오에서도 숫자만 나열하게 된다.

### 나머지 세 파일 (근거용)

**`market-summary.md`** — 숫자를 표로. 네 가지를 모두 채운다.
```markdown
| 숫자 | 무엇을 센 것 | 연도 | 종류 | 출처 | 등급 |
|---|---|---|---|---|---|
| 53.6% | 60대 이상 중 '조작이 어렵다'고 답한 비율 | 2022 | 실측 | 한국소비자원 | A |
```

**`competitor-matrix.md`** — 비슷한 서비스 넷~다섯 × 비교 기준 다섯.
비교 기준은 **0-1단계에서 고른 번호와 1단계 3번 답에서 뽑는다.** 다른 데서 가져오지 않는다.
각 칸에 `🔍 직접 확인` / `📄 검색으로만` 표시와 링크.

**`opportunities.md`** — 비어 있는 자리 **정확히 세 개.** 각각:
- 무엇이 비어 있는가 (한 문장)
- 왜 그렇게 보는가 (비교표의 **어느 칸**에서 나왔는지 지목)
- 여기를 노리면 무엇이 어려울까

셋은 서로 충분히 달라야 한다. 비슷하면 고를 이유가 없다.

## 결과는 아티팩트(볼 수 있는 웹 페이지)로 만든다

**학생은 `.md` 파일을 열지 않는다.** "파일을 확인하세요"로 끝내면 아무도 안 본다.

1. **채팅에 핵심을 먼저 쓴다.** 3~5줄. 아티팩트를 열지 않아도 결론은 알 수 있게.
2. **`Artifact` 도구로 페이지를 발행한다.**
   - 만들기 전에 `artifact-design` 스킬을 먼저 읽는다.
   - 제목은 학생의 프로젝트 이름으로. (예: `반려견 산책 앱 리서치`)
   - `favicon` 이모지 하나, `description` 한 줄.
3. **파일도 폴더에 저장한다.** 근거 보관용이지 학생이 읽을 것은 아니다.
4. **학생에게는 링크를 준다.** 그리고 "이 페이지를 그대로 포트폴리오에 붙이거나, 캡처해서 쓰셔도 됩니다"라고 알린다.

### 아티팩트에 담을 것 — 이 순서를 지킨다

| 순서 | 무엇 | 규칙 |
|---|---|---|
| ① 한눈에 보기 | 제일 중요한 것 3개 | 한 문장씩. 숫자가 있으면 크게 |
| ② 그래서 무슨 뜻인가요 | 해석 | 나열이 아니라 읽어 주기. 이 칸이 비면 실패다 |
| ③ 근거 | 표·출처·페이지 번호 | 접어 두어도 된다. 확인하고 싶은 사람만 편다 |
| ④ 다음에 할 일 | 3개까지 | 오늘 할 수 있는 크기로 쪼갠다 |

**어려운 말을 쓰지 않는다.** 중학생이 읽어도 걸리는 데가 없어야 한다.
전문 용어가 꼭 필요하면 처음 나올 때 괄호로 뜻을 적는다.

---

## 지키는 것

1. **모르면 묻는다** — 필요한 정보가 없으면 질문부터. 추측해서 진행하지 않는다.
2. **근거를 붙인다** — 숫자에는 연도와 출처, 주장에는 어디서 나왔는지.
3. **확실하지 않은 건 따로 모은다** — 맨 뒤 `## 확인 필요` 에. 본문에 섞지 않는다.
4. **길게 쓰지 않는다** — 기회는 셋, 숫자는 셋.
5. **고르는 건 학생이다** — 선택지를 좁혀 주되 결정은 학생이 한다.
6. **쉬운 말로 말한다** — 어려운 용어를 쓸 때는 괄호로 뜻을 붙인다.

결과는 학생이 지금 열어 둔 폴더에 저장한다. 다른 스킬이나 이전 결과물을 필요로 하지 않는다.

## 끝낼 때

먼저 `▓▓▓▓▓▓▓▓▓▓ 100%  |  끝났습니다` 를 출력한다.

`## 확인 필요` 에 검증 못 한 것을 모으고, 아래를 출력한다.

```
넘어가기 전에 확인하세요
□ 00-요약.md 의 '그래서 무슨 뜻인가요' 를 읽고 이해가 되나요?
□ 숫자마다 '무엇을 센 숫자인지' 와 '몇 년 자료인지' 가 다 있나요?
□ 서로 다른 걸 센 숫자를 나란히 놓지 않았나요?
□ 비교표에 🔍(직접 확인) 표시가 하나라도 있나요? 전부 📄면 아직 조사가 아닙니다

숙제
비교표에서 📄로만 채운 칸을 직접 앱을 켜서 확인하고 🔍로 바꾸세요.
이 작업이 리서치를 '검색'에서 '조사'로 바꿉니다.
```
